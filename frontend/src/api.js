// Centralized API base URL configuration
// Empty string = relative URLs, which work through Vite's proxy in dev
// and through any tunnel service (ngrok, localtunnel, etc.)
const API_BASE = '';

export default API_BASE;
